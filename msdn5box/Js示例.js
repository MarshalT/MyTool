/*数学运算*/
function SumNum(x, y) {
    return eval(x) + eval(y);
}
/*字符串运算*/
function SumString(x, y) {
    return x + y;
}
/*Eavl 与Run 执行本方法后请观察结果*/
function Add(Num) {
    return 1 + Num;
}
/*方法中使用字符串*/
function Hellow(x) {
    return 'Hellow' + x;
   套近乎
}